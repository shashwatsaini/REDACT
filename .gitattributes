# Auto detect text files and perform LF normalization
* text=auto
# Track all files inside redact/app/models with LFS
redact/models/** filter=lfs diff=lfs merge=lfs -text
redact/yolo/** filter=lfs diff=lfs merge=lfs -text
